/*Script's Index*/

$(document).ready(function() 
{
	/*Efecto Del Nav*/
	$(".button-collapse").sideNav();

	/* Efecto Parallax*/
	$('.parallax').parallax();

	/* Efecto Carousel */
	$('.carousel').carousel();

	$('.materialboxed').materialbox();

	/* Efecto Collapsible */
	$('.collapsible').collapsible();
});